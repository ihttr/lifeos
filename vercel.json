{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma migrate deploy && tsx scripts/bootstrap-user.mts && next build",
  "framework": "nextjs",
  "regions": ["fra1"]
}
